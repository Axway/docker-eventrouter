streams:
  - name: flow1
    description: "flow1 description"
    reader:
      type: "file_raw_reader"
      conf:
        filenamePrefix: "zoufile"
        readerFilename: "flow1-file_raw_reader"
    writer:
      type: "file_raw_writer"
      conf:
        filenamePrefix: "pathfile"
